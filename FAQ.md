# FAQ

本文收集 RATools-for-PDF 的常见问题、已知限制和维护边界。  
This document collects common questions, known limitations, and maintenance boundaries for RATools-for-PDF.

## 使用与安装

### 我应该下载哪个发布包？

普通用户建议优先使用 GitHub Releases 中的 Windows 打包版本。带更新版会在关于页提供手动检查更新，并可能在重大更新时提醒；`NoUpdate` 版本不会显示更新入口，也不会在启动时访问 GitHub。

### 可以只拷贝 exe 文件使用吗？

不建议。发布包会包含 PySide6、PyMuPDF、qpdf 和其他运行资源，请完整解压并保留目录结构后运行。

### 支持 macOS 或 Linux 吗？

源码主要依赖 Python、PySide6 和 PyMuPDF，部分逻辑可以跨平台运行，但正式分发和验证当前以 Windows 桌面环境为主。macOS / Linux 不是当前维护重点。

## PDF 处理能力

### RATools 会修改原文件吗？

默认不会覆盖原文件，处理结果会输出到 `RATools_Output`。只有用户主动启用“覆盖原始文件”并确认后，才会尝试覆盖源文件。

### 能解除打开密码吗？

不能。`PDF解除权限限制` 仅面向可以正常打开、但存在打印/复制/编辑限制的 PDF，不支持输入或破解打开密码。

### 字体嵌入修复为什么标注为暂不可用或探索状态？

字体问题高度依赖 PDF 结构、原始字体、系统字体和外部工具。当前稳定能力是字体风险预检；自动修复仍属于外部 provider 探索链路，不承诺覆盖复杂 CJK 字体或视觉保真需求。

### 预检结果是否等同于合规审阅？

不是。预检用于发现当前工具可识别的结构问题和建议处理项，不能替代人工合规审阅。复杂、异常或关键递交文件仍建议抽查处理结果。

### 为什么有些规则在智能处理模式下仍会执行？

智能处理模式只会跳过可预检且未命中的规则。无法预检的规则在用户勾选后仍会执行，并在日志中提示相关信息。

## 数据导入导出

### 书签和链接导入导出如何避免同名文件冲突？

向导会按源 PDF 的相对目录层级组织导出的 `CSV` / `JSON` 和导入后的 PDF。因此同时处理不同目录下的同名 PDF 时，数据文件不会互相覆盖。

### 导入时找不到对应数据文件会怎样？

导入前会显示匹配预览；未找到数据文件的 PDF 会被跳过，不会阻止其他已匹配文件继续处理。

## 贡献与支持

### 可以提交功能需求吗？

可以。请使用功能建议模板说明实际工作流、期望交互和范围边界。较大的改动建议先通过 Issue 讨论，再开始实现。

### 维护者是否提供响应 SLA？

没有固定 SLA。该项目按个人维护节奏推进，会优先处理可复现 bug、安全问题、文档修正和范围清晰的改进。

